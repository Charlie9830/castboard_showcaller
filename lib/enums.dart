enum HomePage {
  remote,
  castChanges,
  slides,
  showfile,
}

enum PlaybackAction {
  play,
  pause,
  prev,
  next,
}

enum CastChangePageTab {
  presets,
  castChangeEdit,
}

enum HomeSettingsMenuMode {
  playerSettings,
  castChangeActions,
  generic,
}
