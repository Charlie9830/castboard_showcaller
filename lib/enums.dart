enum HomePage {
  remote,
  castChanges,
  showfile,
}

enum PlaybackAction {
  play,
  pause,
  prev,
  next,
}

enum CastChangePageTab {
  presets,
  castChangeEdit,
}

enum HomeSettingsMenuMode {
  playerSettings,
  castChangeActions,
  generic,
}
